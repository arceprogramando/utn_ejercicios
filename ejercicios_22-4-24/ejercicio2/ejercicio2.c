// Copiar codigo

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;       // Creamos la variable tipo entero (int)
    char caracter; // Creamos la variable tipo char

    // Inicializamos las variables
    num = 10;
    caracter = 'A';

    // Mostramos el valor de esas variables por pantalla

    printf("EL numero es : %i \n", num);
    printf("EL caracter es : %c \n", caracter);

    // system("Pause"); // este comando no sirve para linux
    return 0;
}